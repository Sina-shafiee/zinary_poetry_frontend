export type User = {
  first_name: string;
  last_name: string;
  email: string;
  id: number;
};

export type AuthResponse = {
  first_name: string;
  last_name: string;
  email: string;
  id: number;
};
